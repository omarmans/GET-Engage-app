import { HideLongNamePipe } from './hide-long-name.pipe';

describe('HideLongNamePipe', () => {
  it('create an instance', () => {
    const pipe = new HideLongNamePipe();
    expect(pipe).toBeTruthy();
  });
});
